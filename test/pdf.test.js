var expect = require('chai').expect;
var pdf = require('../functions/pdf.js');
require('dotenv').config();

describe('Use Case 1', () => {     
    it('should pass all tests', async () => {       
        // const res = await pdf.imageToPdf()
        expect(true).to.equal(false)
    })
})